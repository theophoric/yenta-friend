= form_for @conversation, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :participant_ids, :class => 'control-label'
    .controls
      = f.text_field :participant_ids, :class => 'text_field'
  .control-group
    = f.label :_id, :class => 'control-label'
    .controls
      = f.text_field :_id, :class => 'text_field'
  .control-group
    = f.label :_type, :class => 'control-label'
    .controls
      = f.text_field :_type, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), conversation_path, :class => 'btn'

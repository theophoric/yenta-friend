.well[profile]{:class => "gender-#{profile.gender} privacy-#{profile.privacy}"}
  %ul
    = link_to profile_path(profile) do
      %li= image_tag profile.image_url, :size => "40x40"
      %li
        %h4= profile.name
      %li.pull-right{}
    
        - if profile._type[/Chickstud/]
          %span.badge.badge-info= profile.matches.count
        - else
          %span.badge.badge-info= profile.chickstuds.count
  
  
  
